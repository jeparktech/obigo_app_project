import 'package:flutter/material.dart';

// 4. car manager 의 첫 화면 구성
class StartingPage extends StatelessWidget {
  const StartingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
